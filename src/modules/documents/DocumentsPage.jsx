import { useEffect, useState } from 'react'
import AppChrome from '../../components/AppChrome.jsx'
import ModuleHeader from '../../components/ModuleHeader.jsx'
import { documentsApi } from '../../lib/api.js'

export default function DocumentsPage({ setActive }) { const [items,setItems]=useState([]); const [state,setState]=useState('loading'); const fallback=[['Informe de consulta','Mariana Torres','26 ago 2026','Informe clínico','Generado','confirmed'],['Plan de alimentación','Ana Rodríguez','25 ago 2026','Menú semanal','Enviado por WhatsApp','confirmed'],['Informe de consulta','Diego Ramírez','18 ago 2026','Informe clínico','Pendiente de envío','pending']]; useEffect(()=>{documentsApi.list().then(response=>{setItems(response.items||[]);setState('online')}).catch(()=>{setItems([]);setState('demo')})},[]); const rows=items.length?items.map(item=>[item.type==='consultation_report'?'Informe de consulta':'Plan de alimentación',`${item.patient?.firstName||''} ${item.patient?.lastName||''}`,item.generatedAt?new Date(item.generatedAt).toLocaleDateString('es-MX'):'Sin generar',item.type==='consultation_report'?'Informe clínico':'Menú semanal',item.deliveredAt?'Entregado':item.generatedAt?'Generado':'Pendiente','confirmed']):fallback; return <AppChrome active="Documentos" setActive={setActive}><div className="content documents-content"><ModuleHeader eyebrow="DOCUMENTOS · ARCHIVOS CLÍNICOS" title="Documentos" subtitle="Informes y planes vinculados a tus pacientes." action={<div className="module-actions"><span className={'sync-label '+state}>● {state==='online'?'Sincronizados':'Vista demo'}</span><button className="primary" onClick={()=>setActive('Documento')}><span>+</span> Generar documento</button></div>} /><div className="document-stats"><div className="panel"><span className="stat-icon mint">▣</span><div><b>{rows.length}</b><small>Documentos disponibles</small></div></div><div className="panel"><span className="stat-icon orange">↗</span><div><b>{rows.filter(x=>x[4].includes('Pendiente')).length}</b><small>Pendientes de envío</small></div></div><div className="panel"><span className="stat-icon purple">✓</span><div><b>{rows.filter(x=>x[4].includes('Generado')||x[4].includes('Entregado')).length}</b><small>Generados</small></div></div></div><div className="documents-toolbar panel"><div className="search-field">⌕ <input placeholder="Buscar por paciente o documento..." /></div><div className="view-switch"><button className="selected">Todos</button><button>Informes</button><button>Planes</button></div></div><div className="documents-table panel"><div className="documents-head"><span>Documento</span><span>Paciente</span><span>Fecha</span><span>Estado</span><span /></div>{rows.map(row=><div className="documents-row" key={row[0]+row[1]}><span className="document-name"><i>▤</i><span><b>{row[0]}</b><small>{row[3]}</small></span></span><span className="muted">{row[1]}</span><span className="muted">{row[2]}</span><span className={'status '+(row[5]==='confirmed'?'confirmed':'pending')}>{row[4]}</span><button className="row-arrow" onClick={()=>setActive('Documento')}>→</button></div>)}</div></div></AppChrome> }
