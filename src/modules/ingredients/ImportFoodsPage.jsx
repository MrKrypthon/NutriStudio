import { useState } from 'react'
import AppChrome from '../../components/AppChrome.jsx'
import ModuleHeader from '../../components/ModuleHeader.jsx'
import { foodApi, ingredientsApi } from '../../lib/api.js'

export default function ImportFoodsPage({ setActive }) { const [query,setQuery]=useState(''); const [source,setSource]=useState('all'); const [items,setItems]=useState([]); const [selected,setSelected]=useState(null); const [group,setGroup]=useState('Cereales'); const [saved,setSaved]=useState(false); const [translatedFrom,setTranslatedFrom]=useState(null); const search=async(e)=>{e.preventDefault();if(query.trim().length<2)return;const result=await foodApi.search(query,source).catch(()=>({items:[]}));setItems(result.items||[]);setTranslatedFrom(result.translatedQuery||null)}; const save=async()=>{if(!selected)return;await ingredientsApi.import({externalId:selected.externalId,source:selected.source,name:selected.name,group,unit:'g',nutrition:selected.nutrition,equivalence:{group,serving:'100 g'}}).catch(()=>null);setSaved(true)}; return <AppChrome active="Importar alimentos" setActive={setActive}><div className="content import-review"><ModuleHeader eyebrow="CATÁLOGO PROPIO · REVISIÓN" title="Revisar alimento" subtitle="Selecciona una fuente y valida los datos antes de guardarlos localmente." action={<button className="secondary" onClick={()=>setActive('Ingredientes')}>Ver catálogo</button>} /><form className="import-search panel" onSubmit={search}><div className="search-field">⌕ <input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Busca un alimento..." /></div><select value={source} onChange={e=>setSource(e.target.value)}><option value="all">Todas las fuentes</option><option value="usda">USDA</option><option value="openfoodfacts">Open Food Facts</option></select><button className="primary">Buscar <span>→</span></button></form>{translatedFrom&&<p className="muted" style={{margin:'-10px 0 16px'}}>Buscado como "{translatedFrom}" (traducción automática del catálogo local)</p>}<div className="review-layout"><section><div className="import-results-head"><b>{items.length||0} resultados</b><span>Selecciona un alimento para revisar</span></div>{items.map(item=><button className={'review-result panel '+(selected?.externalId===item.externalId?'selected':'')} onClick={()=>setSelected(item)} key={item.source+item.externalId}><span className="import-result-icon">{item.imageUrl?<img src={item.imageUrl} alt="" />:'◉'}</span><span><b>{item.name}</b><small>{item.source} · {item.serving.quantity}{item.serving.unit}</small></span><span className="muted">{Math.round(item.nutrition.kcal||0)} kcal</span></button>)}{!items.length&&<div className="import-empty panel"><span>⌕</span><h2>Busca para ver resultados</h2><p>El alimento aún no se agregará hasta que lo revises.</p></div>}</section><aside className="review-panel panel">{selected&&!saved?<><p className="eyebrow">REVISIÓN PROFESIONAL</p><h2>{selected.name}</h2><span className="source-badge">Fuente: {selected.source}</span><div className="review-nutrients"><div><small>Energía</small><b>{Math.round(selected.nutrition.kcal||0)} kcal</b></div><div><small>Proteína</small><b>{Number(selected.nutrition.protein||0).toFixed(1)} g</b></div><div><small>Carbohidratos</small><b>{Number(selected.nutrition.carbs||0).toFixed(1)} g</b></div><div><small>Grasas</small><b>{Number(selected.nutrition.fat||0).toFixed(1)} g</b></div></div><label>Grupo alimentario<select value={group} onChange={e=>setGroup(e.target.value)}><option>Verduras</option><option>Frutas</option><option>Cereales</option><option>Proteínas</option><option>Lácteos</option><option>Grasas</option></select></label><label>Porción equivalente<input defaultValue="100 g" /></label><div className="review-warning">ⓘ Revisa unidad, porción y grupo antes de importarlo al sistema de equivalentes.</div><button className="primary full-button" onClick={save}>Guardar en mi catálogo <span>→</span></button></>:saved?<div className="saved-result"><span>✓</span><h2>Alimento guardado</h2><p>Ya puedes usarlo en tus recetas y planes nutricionales.</p><button className="primary" onClick={()=>setActive('Ingredientes')}>Ir a ingredientes <span>→</span></button></div>:<div className="result-empty"><span>◌</span><h3>Selecciona un resultado</h3><p>Aquí podrás validar nutrientes y equivalencias antes de guardar.</p></div>}</aside></div></div></AppChrome> }
